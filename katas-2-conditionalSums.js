const conditionalSum = function(values, condition) {
  // Your code here
  for (let i = 0; i <= values.length; i++){
    let evenNumbers = [];
    if (conditionalSum[1] === "even"){
      i % 2 === 0;
      evenNumbers.push[i];
      return evenNumbers;
    } 
  console.log(evenNumbers);
  let oddNumbers = [];
    if (conditionalSum[1] === "odd"){
      i % 2 !== 0;
      oddNumbers.push[i];
      return oddNumbers;
    }
  console.log(oddNumbers);
  };
};

console.log(conditionalSum([1, 2, 3, 4, 5], "even"));
console.log(conditionalSum([1, 2, 3, 4, 5], "odd"));
console.log(conditionalSum([13, 88, 12, 44, 99], "even"));
console.log(conditionalSum([], "odd"));

// set sum to equal zero
// check the condition
// loop through the array and fine each element that satisfies the condition
// add such elements to the sum 

// Do same for the second condition
// finally output the sum.